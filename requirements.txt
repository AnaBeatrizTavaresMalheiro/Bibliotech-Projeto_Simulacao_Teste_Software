# Framework principal da API
fastapi==0.115.4

# Servidor ASGI para rodar o FastAPI localmente
uvicorn[standard]==0.30.6

# ORM e integração com SQLite (baseado em SQLAlchemy + Pydantic)
sqlmodel==0.0.22

# Validação e serialização de dados
pydantic==2.9.2
pydantic-core==2.23.4

# Suporte a requisições multipart (necessário para formulários e uploads)
python-multipart==0.0.9

# Ferramenta opcional de formatação e padronização de código
black==24.8.0
isort==5.13.2

# Testes automatizados (opcional, útil se for testar com Pytest)
pytest==8.3.2
httpx==0.27.0
